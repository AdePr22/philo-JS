// Membahas arguments dan Parameters yang ada di dalam function js

/*
    argument adalah nilai yang dikirimkan ke parameter saat fungsi di panggil.
*/
var ox = (nama) => {
  return nama;
};
console.log(ox("dew"));
